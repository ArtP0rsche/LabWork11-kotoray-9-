name: build
on:
  push:
    branches: ["main"]
jobs:
  build:
    name: Build C# Program
    runs-on: self-hosted
    steps:
      - name: checkout repo
        run: actions/checkout@v3
      - name: dotnet
        run: dotnet build
